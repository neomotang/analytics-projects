# Data analytics projects
Examples of some data analytics projects I have completed.

---

- **bike-trips** - extract and transform data in a `Jupyter` notebook using `Python`, and create report in `Tableau`
- **coffee-sales** - extract and transform `MS Excel` data in `Power Query`, and report insights in `Power BI`
